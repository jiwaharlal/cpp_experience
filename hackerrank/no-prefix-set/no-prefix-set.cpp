#include <iostream>

int main()
{
    int n;
    std::cin >> n;

    std::vector<std::array<int, 8>> tree(n * 60, {0, 0, 0, 0, 0, 0, 0, 0});

    std::string str;
    for (int i = 0; i < n; ++i)
    {
        std::cin >> str;
        for (auto c : str)
        {
        }
    }
}
